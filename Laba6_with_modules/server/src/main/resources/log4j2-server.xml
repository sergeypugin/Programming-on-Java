<?xml version="1.0" encoding="UTF-8"?>
<!-- Настройка логгера для сервера -->
<Configuration status="WARN">
    <Appenders>
        <!-- Вывод логов в консоль администратора сервера -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!-- Запись логов в файл server.log в корне проекта -->
        <File name="LogFile" fileName="server.log" append="true">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </File>
    </Appenders>

    <Loggers>
        <!-- Базовый уровень логирования INFO -->
        <Root level="info">
            <!-- Привязываем оба аппендера чтобы логи шли и на экран и в файл -->
            <AppenderRef ref="Console"/>
            <AppenderRef ref="LogFile"/>
        </Root>
    </Loggers>
</Configuration>